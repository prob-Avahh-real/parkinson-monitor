# ParkinsonMonitor Agent 服务依赖
# Python 3.11+

# Supabase 数据接入
supabase>=2.5.0

# LLM
openai>=1.55.0

# 配置
python-dotenv>=1.0.0

# PDF 报告 (可选，用于生成 PDF)
reportlab>=4.2.0

# HTTP 请求 (webhook 通知)
requests>=2.32.0

# 定时调度 (可选，用于 cron 替代方案)
# schedule>=1.2.0
